model_list:
  - model_name: benchmark-mock
    litellm_params:
      model: openai/benchmark-mock
      api_base: http://rustygate:8080/v1
      api_key: benchmark-key

general_settings:
  master_key: benchmark-key
